
# Linux Backup & Monitoring Tool

## Features
- Automated backups with rsync + tar
- Disk usage monitoring
- Cron job scheduling
- Logging with alerts

## Usage
1. Clone repo
2. Edit `src` and `dest` paths in `backup.sh`
3. Run manually or schedule with cron
